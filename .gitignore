# npm workspaces hoist shared dependencies here.
node_modules/

# Environment — never commit real secrets
.env
.env.*
!.env.example

# Build output
dist/
coverage/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Vercel CLI: project link and the OIDC token it pulls
.vercel

# Real secrets never get committed, but the templates that document them must.
.env*
!.env.example
